table:
  name: org_usage_events
  schema: public
object_relationships:
  - name: org
    using:
      foreign_key_constraint_on: org_id
select_permissions:
  - role: user
    permission:
      columns: '*'
      filter:
        org:
          members:
            user_id:
              _eq: X-Hasura-User-Id
